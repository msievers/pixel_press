require "spec_helper"

describe PixelPress do
  it "has a version number" do
    expect(PixelPress::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
